apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: signoz-otel-ingressroute
  namespace: signoz
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`otel.local.net`)
      services:
        - name: signoz-helm-release-otel-collector.signoz
          port: 4318
