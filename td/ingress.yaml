apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: td-ingressroute
  namespace: td
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`doit-mcp.int.mvaldes.dev`)
      services:
        - name: td-svc-mcp
          port: 80
