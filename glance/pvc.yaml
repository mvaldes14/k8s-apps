apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myapp
  namespace: default
  labels:
    app: myapp
spec:
  # AKS: default,managed-premium
  # GKE: standard
  # EKS: gp2 (custom)
  # Rook: rook-ceph-block,rook-ceph-fs
  storageClassName: 
  accessModes:
  - 
  resources:
    requests:
      storage: 2Gi
