apiVersion: v1
kind: Service
metadata:
  name: otel-isp-monitor-svc
  namespace: signoz
spec:
  selector:
    app: otel-isp-monitor
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
