apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: homelab-db-daily
spec:
  # 6-field cron (leading seconds field). Daily at 03:00.
  schedule: "0 0 3 * * *"
  backupOwnerReference: self
  cluster:
    name: homelab-db
