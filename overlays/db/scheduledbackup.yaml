apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: homelab-db-48h
spec:
  # every 48h except at the end of 31-day months, where the 31st and
  schedule: "0 0 3 */2 * *"
  backupOwnerReference: self
  method: barmanObjectStore
  immediate: true
  cluster:
    name: homelab-db
