apiVersion: v1
kind: Service
metadata:
  name: authentik-svc
  namespace: authentik
spec:
  selector:
    app: authentik-server-deploy
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9000
