apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudflared-flux
spec:
  interval: 30m
  sourceRef:
    kind: GitRepository
    name: homelab-repository
  path: "./overlays/cloudflared"
  prune: true
  timeout: 3m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
