apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # sources first: GitRepositories + auth secret the Kustomizations below reference
  - sources/
  - kustomizations/
  - notifications/
  - images/
