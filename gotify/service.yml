apiVersion: v1
kind: Service
metadata:
  name: gotify-svc
  namespace: gotify
spec:
  selector:
    app: gotify-deploy
  type: ClusterIP
  ports:
  - name: myjob
    protocol: TCP
    port: 80
    targetPort: 80
