apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-pvc 
  namespace: linkding 
spec:
  storageClassName: nfs-k8s-keep
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
